# Canonical public origin. Set this in Vercel (Production + Preview) once the
# real domain is attached — it drives metadataBase, canonical URLs, Open Graph
# URLs, sitemap.xml and robots.txt. Without it, builds fall back to the Vercel
# deployment URL.
NEXT_PUBLIC_SITE_URL=https://oragonlabs.com
